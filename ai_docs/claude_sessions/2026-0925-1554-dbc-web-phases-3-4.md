# dbc web, Phases 3 and 4: the real editor and grid, and explain

Session: c79a056f-17d1-4002-ad13-42ff58bb18e8
Date: 2026-09-25

## Ask

"Do phases 3 and 4 committing inbetween each" — of `ai_docs/plans/web-ui.md`.

- Phase 3: Monaco with SQL highlighting and the TUI's keys, the
  statement-under-caret marker, multi-statement progress; the virtualized
  grid (paging, sort, hide/show, resize, range, inspect); copy in every
  format (rich HTML via the Clipboard API), export as downloads, history,
  tables with preview. Done when the TUI's grid tests have browser
  equivalents.
- Phase 4: the Plan tab with full parity with the HTML plan page — its
  script lifted into one `plan.js` both load — plus the web's extras. Done
  when the checklist is ticked in headless Chrome against the fixtures.

## Commits

- `98ae678` dbc web Phase 3: the real editor and grid
- `a70cc46` dbc web Phase 4: explain, with everything the plan page does

## Phase 3 — what was built

| Piece | Where |
|---|---|
| Monaco 0.52.2 vendored, trimmed to the SQL editor's files (4.2 MB, not 14) | `scripts/vendor_monaco.sh`, `web/static/vendor/monaco/` |
| Editor: textarea stays the source of truth, Monaco mirrors into it; dialect by driver; palette from `/theme.css`; chords bound to ⌘ and (on a Mac) the real Ctrl; same-origin worker bootstrap | `web/static/js/editor.js`, `monaco-worker.js` |
| Statement marker from the server's splitter, UTF-16 offsets | `POST /api/v1/stmt` (`web/grid.go`) |
| Grid, virtualized both ways, pages of 200, TUI gestures/keys/words, layout kept across a same-columns rerun, own double-click counting | `web/static/js/grid.js` |
| Paged JSON results, copy (seq-checked, 409 when stale), export downloads, history, table preview | `web/grid.go` |
| Split page script: `core.js` (api/log/state/cmd table), `ui.js` (menus, dialogs, clipboard with promise-valued `ClipboardItem`), `app.js` (boot, SSE, commands, keys, history modal, sidebar) | `web/static/js/` |
| Shared view arithmetic: `workspace.SortRows`, `workspace.Project` (the TUI grid now calls them); `export.PlainCells` | `workspace/view.go`, `export/export.go` |
| Multi-statement progress "· 2/4" in the status, both UIs | `workspace.RunProgress`, `runningStatusLocked` |

Removed `web/pages/results.go` (the Phase 2 HTML table) — results are JSON
now. CSP `style-src` gained `'unsafe-inline'` (Monaco's `<style>`, the grid's
placement); `script-src` stays `'self'`.

Found by the browser run and fixed: the resize handle straddled the header
cell's clipped edge (half of it sorted); canvas-measured char width made
columns short (canvas doesn't resolve `ui-monospace`) — measured in the
grid's own DOM font now; native `dblclick` unreliable because a press
redraws the rows.

## Phase 4 — what was built

| Piece | Where |
|---|---|
| The view: `DbcPlan.mount(root, doc, opts)`, scoped (`data-p` parts, no ids), per-mount state, `destroy`; options `embedded`, `keys`, `copy`, `onInsert`, `compare`, `actions`, `hash` | `explain/assets/plan.js` |
| Its CSS under `.dbc-plan`, light palette per view, container query for the narrow layout | `explain/assets/plan.css` |
| Standalone page = template inlining both; `explain.PlanJS/PlanCSS` exported; `explain.ScriptHash()` for a hash-only CSP | `explain/assets/plan.html`, `explain/html.go` |
| Web endpoints: explain (again = the plan's own statement), plan, plan/text (text or raw), plan.html (own CSP, `?download=1`); SSE `explain`; plan + "before" kept per tab | `web/plan.go`, `web/hub.go` |
| Plan tab glue: tabs, Ctrl+X (nothing selected) / Ctrl+Shift+X / Alt+X / ◈ Explain, `e a y Y b p`, ⤓ Insert, header action buttons | `web/static/js/planview.js` |
| Shared words: `explain.SameSubject`, `explain.Compare`, `Plan.Findings`, `workspace.PlanNotes`, `workspace.PlanStatus` (TUI now calls them) | `explain/compare.go`, `workspace/plannotes.go` |

The page's command hover now carries the full EXPLAIN (it said only "The
EXPLAIN dbc sent" before). Deferred to Phase 5: "✦ ask the assistant" hooks.

## Verification

- `go vet ./...`, `gofmt -l .` clean; `go test ./...` green (CATS_* stripped);
  `-race -count=3` on `web`, `workspace`, `explain`, `tui`.
- New tests: `workspace/view_test.go` (SortRows, Project, progress);
  `web/grid_test.go` (sort, pages and display cap, range/hidden copy and
  words, HTML flavor, bad requests, stale 409, export for every format,
  history + preview, UTF-16 marker range, "· 2/3" progress);
  `web/plan_test.go` (explain → plan → again with before, plan text/raw,
  typed EXPLAIN, refusals incl. busy 409, served page CSP + download, shared
  assets byte for byte); `explain/html_test.go` (inlined view, no
  `</script`, one executable script matching `ScriptHash`).
- Live `db` tests: 13/13 against Postgres 17 and MySQL 8.4 (docker).
- Headless Chrome (go-rod, scratch programs): Phase 3 — Monaco under the
  CSP, marker, Ctrl+Enter, sort cycle, shift-click range → CSV on the real
  clipboard, `y`/`Y`, HTML flavor, inspect (JSON), resize/fit, hide survives
  rerun, `-`/`+`, history, preview, CSV download, 1,000 rows virtual, 420 px.
  Phase 4 — 283 checks: the whole parity checklist on five fixture pages
  and in the web tab on live Postgres (analyzed), MySQL, SQLite, bytdb; the
  extras incl. Insert → CREATE INDEX → analyze again → "was 30.1 ms → 87 µs
  ▼100%", the standalone page under the hash-only CSP. Consoles clean.

## Docs and memory

- `ai_docs/plans/web-ui.md`: Phases 3 and 4 marked done with outcome
  tables, decisions and verification.
- Memory `web-e2e-harness`: how the browser checks are run, and rod /
  Monaco pitfalls (rod releases held keys; click the grid by coordinates;
  ⌘ vs Ctrl in Monaco on a Mac).

## Next

Closed: N-046. Declined: None. Raised: N-051, N-052.
Deferred: None. Promoted: None.
Updated: N-048. Full list: `ai_docs/todo/next-list.md`.
