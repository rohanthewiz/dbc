# Tag the v0.1.0 release

Session: 24071d22-9de3-4138-8b51-be808cd79a77
Date: 2026-09-25

## Ask

Next-list item **N-011**: tag a release. The push half was already done
(N-019). The repo had no tags, `cats-plugin.toml` declares
`version = "0.1.0"`, and the docs point at
`go install github.com/rohanthewiz/dbc@latest`, which resolved only to a
pseudo-version of `main`. A `v0.1.0` tag lines the three up.

## Premise check

- `git tag -l` was empty, and `main` was even with `origin/main` at `aec93c8`
  (the merge of PR #1, explain).
- `go.mod` has no `replace` directives. A `replace` would make
  `go install …@latest` refuse to build the module.
- dbc has no version string of its own (no `--version` flag, no `Version`
  var), so nothing in the code had to change to match the tag.
- `go build ./...` and `go vet ./...` passed. `go test ./...` passed, run with
  the `CATS_*` variables removed because this session runs inside a cats pane.

## Change

- Created the annotated tag `v0.1.0` on `aec93c8`. Its message ties it to
  `cats-plugin.toml`'s version and to `@latest`.
- Pushed it after the user confirmed. A version on proxy.golang.org is
  immutable once fetched, so any fix now ships as `v0.1.1`.
- Checked from outside the module (in the scratchpad):
  `go list -m github.com/rohanthewiz/dbc@latest` → `v0.1.0`.
- `ai_docs/todo/next-list.md`: moved N-011 from Open to the top of Closed,
  with a note to tag again whenever `cats-plugin.toml`'s version changes.

No code changed.

## Next

Closed: N-011. Declined: None. Raised: None.
Deferred: None. Promoted: None.
Updated: None. Full list: `ai_docs/todo/next-list.md`.
